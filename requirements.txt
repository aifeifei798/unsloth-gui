# unsloth 会自动带上训练栈（torch / transformers / trl / peft / datasets /
# accelerate / unsloth_zoo 等），不用手动 pin，跟着 unsloth 走就行。
# 安装：
#   pip install -r requirements.txt
# 或（推荐，自动匹配 torch 后端）：
#   uv venv --python 3.13 && source .venv/bin/activate
#   uv pip install -r requirements.txt --torch-backend=auto

# --- 核心：训练/推理（torch 由它自动解决） ---
unsloth
bitsandbytes>=0.43

# --- 本项目直接用的：UI / 监控 / 下载加速 / 魔搭 ---
gradio>=6.27
tensorboard
hf_transfer
modelscope
